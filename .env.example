# =============================================================================
# Database (used by docker-compose postgres service)
# =============================================================================
POSTGRES_USER=fotomalovanky
POSTGRES_PASSWORD=fotomalovanky
POSTGRES_DB=fotomalovanky

# Local development DATABASE_URL (for running alembic outside docker)
# Used when running: uv run alembic upgrade head
DATABASE_URL=postgresql+asyncpg://fotomalovanky:fotomalovanky@localhost:5439/fotomalovanky

# =============================================================================
# S3 Storage (MinIO local dev / Cloudflare R2 production)
# =============================================================================
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=fotomalovanky
S3_REGION=auto
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_FORCE_PATH_STYLE=true
# Public URL for accessing files (MANDATORY - no fallbacks)
S3_PUBLIC_URL=http://localhost:9000/fotomalovanky

# =============================================================================
# Mercure (Real-time SSE)
# Generate keys with: openssl rand -base64 32
# =============================================================================
MERCURE_PUBLISHER_JWT_KEY=change-me-publisher-secret-key
MERCURE_SUBSCRIBER_JWT_KEY=change-me-subscriber-secret-key
MERCURE_URL=http://localhost:3000/.well-known/mercure

# =============================================================================
# Shopify
# =============================================================================
SHOPIFY_STORE_URL=https://aqi8it-7n.myshopify.com
SHOPIFY_STORE_HANDLE=aqi8it-7n
SHOPIFY_ACCESS_TOKEN=shpat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SHOPIFY_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Application
# =============================================================================
DEBUG=true
LOG_LEVEL=debug
BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173  # direnv: optional

# =============================================================================
# Frontend (Vite environment variables)
# =============================================================================
# VITE_API_URL is the base URL for API requests (without /api/v1 suffix)
# The Orval-generated code already includes /api/v1 in paths
# Leave empty for local dev with Vite proxy, set to backend URL for Docker
VITE_API_URL=http://localhost:8000

# =============================================================================
# Download Proxies (residential proxy for CDN bypass)
# =============================================================================
# Supports multiple proxies - numbered PROXY_1_*, PROXY_2_*, etc.
# All fields required except certificate (optional)

# BrightData residential proxy example
# PROXY_1_HOST=brd.superproxy.io
# PROXY_1_PORT=22225
# PROXY_1_USERNAME=brd-customer-xxx-zone-residential
# PROXY_1_PASSWORD=your_password
# PROXY_1_CERTIFICATE=brightdata.crt

# Optional second proxy
# PROXY_2_HOST=proxy2.example.com
# PROXY_2_PORT=8080
# PROXY_2_USERNAME=user
# PROXY_2_PASSWORD=pass
