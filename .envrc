if command -v load-onepassword-env.sh &>/dev/null; then
    source load-onepassword-env.sh fotomalovanky-admin .env
    source load-onepassword-env.sh fotomalovanky-admin/backend backend/.env
else
    dotenv_if_exists .env
    dotenv_if_exists backend/.env
fi

export UV_PROJECT=backend
